version: 2
jobs:
  deploy:
    steps:
      - checkout
      - run:
          name: copy
          command: scp -r * admin_ftp_11@vps-1049136-17732.host4g.ru:/mishka

workflows:
  version: 2
  release:
    jobs:
    - deploy:
        filters:
          branches:
            only: master